package com.cx.configprovider.services;

import com.cx.configprovider.dto.ConfigLocation;

class GitHubDownloader implements SourceDownloader {
    @Override
    public String downloadFileContent(ConfigLocation configLocation) {
        return "content stub";
    }
}
